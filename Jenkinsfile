pipline {
    agent any

    stages {
        stage("init"){
            script{
                echo "initialisation of project"
            }
        }

        stage("build"){
            script{
                echo "build the project"
            }
        }

        stage("test"){
            script{
                echo "test the project"
            }
        }

        stage("deploy"){
            script{
                echo "test the project"
            }
        }
    }
}